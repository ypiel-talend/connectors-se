// this tells the framework in which family (group of components) and categories (UI grouping)
// the components in the nested packages belong to
@Components(family = "CompanyFamily", categories = "Misc")
@Icon(value = Icon.IconType.CUSTOM, custom = "CompanyFamily")
package com.company.talend.components;

import org.talend.sdk.component.api.component.Components;
import org.talend.sdk.component.api.component.Icon;

